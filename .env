# === Backend (Postgres) ===
# Local default (matches docker-compose.yml):
DATABASE_URL=postgresql://postgres:0810@localhost:5432/restaurant

# Server
PORT=5000
HOST=0.0.0.0

# JWT
# IMPORTANT: change this in real usage
JWT_SECRET=1D/8WywTELajDdyNsS7vc3dqRvVuJZlzW1ye+eWYg5vsOtd9mfOwvIWb/puWIk+m
JWT_EXPIRES_IN=15m

# === Frontend (Vite) ===
# "local" = offline/localStorage (modo atual - não quebra nada)
# "api"   = usa backend endpoints (full stack)
VITE_BACKEND_MODE=api

# Optional base URL. Leave empty for same-origin.
VITE_API_BASE_URL=
